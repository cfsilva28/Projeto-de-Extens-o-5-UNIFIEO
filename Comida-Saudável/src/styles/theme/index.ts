import { colors } from "../colors";

// eslint-disable-next-line
export default {
    colors: {
        backgroundWhite: colors.BACKGROUNDWHITE,
        backgroundGray: colors.BACKGROUNDGRAY,
        textColor: colors.TEXTCOLOR,
        gray: colors.GRAY,
        lightGray: colors.LIGHTGRAY,
        green: colors.GREEN
    }
}